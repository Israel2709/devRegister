function getSelection(selectedOption,parameter){
	console.log("click")
	var text = $(selectedOption).text()
	console.log(selectedDeveloper)
	console.log(text)
}
var database = firebase.database();

var selectedDeveloper
